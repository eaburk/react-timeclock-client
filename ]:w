import React, { useState, useEffect } from 'react';
import { Modal, Button, Form } from 'react-bootstrap';
import { useTimeStore } from '../hooks';
import type { TimeEntry } from '../types';
import { updateTimeEntry } from '../services';

type EditCompanyModalProps = {
  show: boolean;
  handleClose: () => void;
};

const EditCompanyModal: React.FC<EditCompanyModalProps> = ({ show, handleClose }) => {
  [newCompanyName, setNewCompanyName] = useState('');
  const handleNewCompanyNameChange = (event) => {
    setNewCompanyNameChange(event.target.value);
  }
  return (
    <Modal show={show} onHide={handleClose}>
      <Modal.Header closeButton>
        <Modal.Title>Edit Companies</Modal.Title>
      </Modal.Header>
      <Modal.Body>
        <Input type="text" onChange={event => handleCompanyNameChange(event)}>
          
        </Input>
      </Modal.Body>
      <Modal.Footer>
        <Button variant="secondary" onClick={handleClose}>
          Cancel
        </Button>
      </Modal.Footer>
    </Modal>
  );
};

export default EditTimeModal;
